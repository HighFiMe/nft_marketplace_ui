{
  "name": "graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ lazycoder1/graph",
    "create-local": "graph create --node http://localhost:8020/ lazycoder1/graph",
    "remove-local": "graph remove --node http://localhost:8020/ lazycoder1/graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lazycoder1/graph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.2",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
